namespace FileTree.Core.Models;

/// <summary>
/// Style for marking hidden nodes.
/// </summary>
public enum HiddenStyle
{
    /// <summary>[hidden] prefix.</summary>
    Prefix,
    /// <summary>(hidden) suffix.</summary>
    Suffix,
    /// <summary>* minimal marker.</summary>
    Minimal
}

